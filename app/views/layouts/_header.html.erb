<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">PThinh</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav"> 
        <li class="nav-item"><%= link_to "Home",   '/', style: "text-decoration: none; color: #000; padding: 0 10px" %></li>
        <li class="nav-item"><%= link_to "Help",   '/help' , style: "text-decoration: none; color: #000; padding: 0 10px"%></li> 
      </ul>
    </div>
    <div style="padding-right: 30px">
        <% if current_user %>
          <span style= " font-size:1.3rem; padding-right: 20px;">
            <%= link_to "admin", current_user.name, style: 'text-decoration: none;color: #000;' %>
            <i class="fa-regular fa-user"></i>
          </span> 
          <%= link_to '<i class="fa-solid fa-right-to-bracket"></i>'.html_safe, logout_path %>
        <% else %>
          <%= link_to "Sign Up", signup_path %> or
          <%= link_to "Log In", login_path %>
        <% end %>
    </div>
  </div>
</nav>
